- 选项卡片组件（MathOptionCard）采用索引取模算法（index % colors.length）自动分配 pastel 色系背景，确保视觉多样性与代码简洁性。
- 所有题型渲染器（Renderers）遵循统一的 Props 契约，接收 data, options, optionStates 和 onSelect，实现视图层与业务逻辑的解耦。
- 反馈系统采用延迟触发机制（setTimeout 400ms），确保用户先看到选项的状态变化（如变红/变绿），再弹出全屏鼓励浮层，增强交互节奏感。