首次运行前需联网执行 `pip install edge-tts`；更新数据后按 `AUDIO.md` 表格选择对应脚本，例如 `python3 scripts/generate-char-audio.py` 或 `python3 scripts/generate-chinese-questions.py`；生成的 `public/audio/` 必须 `git add` 提交，否则 Vercel 线上缺失音频。