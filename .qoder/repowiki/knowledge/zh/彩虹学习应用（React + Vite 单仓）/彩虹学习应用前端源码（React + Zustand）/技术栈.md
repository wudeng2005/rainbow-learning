React 18 + TypeScript + Vite（从 `main.tsx` 的 `createRoot` 与 `.tsx` 扩展名推断）；路由使用 `react-router-dom`；全局状态使用 `zustand` + `persist` 中间件；UI 样式采用 Tailwind CSS（类名如 `min-h-dvh`、`animate-bounce`）；数据持久化同时支持 localStorage 与 Supabase（`lib/supabase.ts`）。