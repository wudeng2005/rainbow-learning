开发：`npm run dev`；构建：`npm run build`（先 `tsc -b` 再 `vite build`）；预览：`npm run preview`。音频资源需运行 `scripts/generate-audio.py`、`generate-char-audio.py`、`generate-story-audio.py` 等 Python 脚本生成后放入 `public/audio/`。