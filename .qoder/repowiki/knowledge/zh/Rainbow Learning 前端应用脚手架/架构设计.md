- 采用 Vite 作为核心构建工具，通过 `vite.config.ts` 统一配置 React 插件与 Tailwind CSS 处理器。
- 利用 TypeScript 项目引用（Project References）机制，在 `tsconfig.json` 中解耦应用代码与 Node 环境配置的类型检查边界。
- 通过 `package.json` 定义标准化的开发工作流脚本（dev, build, lint），确保子模块在统一的工程规范下运行。