- 所有 Store 均导出 `hydrate` 方法，用于在应用初始化或数据同步完成后，用云端最新数据覆盖本地 Zustand 状态。
- Store 中的写操作（如 `addGems`, `recordError`）在更新本地状态的同时，会调用 `syncManager` 将变更记录暂存至待同步队列，而非立即发起网络请求。
- 日期处理统一使用 `YYYY-MM-DD` 格式的字符串，并通过 `src/lib/storage.ts` 提供的 `getTodayStr` 和 `addDays` 工具函数进行操作。