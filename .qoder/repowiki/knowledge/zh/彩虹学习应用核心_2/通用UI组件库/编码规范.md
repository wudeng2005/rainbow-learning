- 所有交互型组件均定义统一的状态枚举（如 'idle', 'correct', 'wrong'），并基于状态驱动样式与动画切换。
- 动画实现遵循统一的物理参数配置，普遍采用 type: 'spring' 配合特定的 stiffness 和 damping 值以保证手感一致性。
- 组件接口设计遵循事件驱动原则，通过 onSelect 或 onContinue 等回调函数将交互逻辑委托给父组件。
- 视觉反馈组件（如进度条、反馈遮罩）在通用版与数学版之间保持结构一致，仅替换 Emoji 图标与背景渐变色以匹配学科氛围。