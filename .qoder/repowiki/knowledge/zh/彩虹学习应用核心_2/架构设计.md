- `useAppInit` 在应用挂载时执行一次性数据迁移（localStorage 到 Supabase）并拉取最新状态，随后注入各业务 Store。
- `App.tsx` 根据初始化状态展示加载屏或路由视图，其中 `/learn` 采用独立全屏布局，其余页面嵌套于 `Layout` 以共享导航结构。
- 注册 `visibilitychange` 监听器，确保应用在后台切换回前台时自动触发数据同步，维持多端状态一致性。