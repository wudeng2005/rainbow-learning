- 入口编排：`main.tsx` 挂载 `App`，`App.tsx` 调用 `useAppInit` 阻塞渲染直至数据就绪。
- 跨模块桥接：`useAppInit` 作为唯一桥梁，调用 `app_core` 的数据获取接口（`lib/db`）并将结果注入全局 Store，同时依赖 `shared_ui` 的 `Layout` 包裹所有业务页面。
- 离线优先策略：在初始化阶段执行幂等的 `localStorage` 到 Supabase 迁移，并注册页面可见性监听以触发后台同步，确保多端数据一致性。